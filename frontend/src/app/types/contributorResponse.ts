export type ContributorPostResponse = {
  name: string;
  phone: string;
  is_active: boolean;
};

export type ContributorGetResponse = {
  id: string;
  name: string;
  phone: string;
  is_active: boolean;
};
