export type ContributorPostResponse = {
  name: string;
  phoneNumber: string;
  isActive: boolean;
};

export type ContributorGetResponse = {
  id: string;
  name: string;
  phoneNumber: string;
  isActive: boolean;
};
